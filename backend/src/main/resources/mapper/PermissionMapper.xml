<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docreview.mapper.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.docreview.entity.Permission">
        <id column="id" property="id"/>
        <result column="perm_name" property="permName"/>
        <result column="perm_code" property="permCode"/>
        <result column="resource_type" property="resourceType"/>
        <result column="resource_url" property="resourceUrl"/>
        <result column="method" property="method"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.deleted = 0
        ORDER BY p.id
    </select>

    <!-- 根据角色ID查询权限ID列表 -->
    <select id="selectPermIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id
        FROM sys_role_permission
        WHERE role_id = #{roleId}
    </select>

</mapper>
